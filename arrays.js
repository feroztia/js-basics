/*
const stringValue = 'hello world';
const anotherStringValue = "hello world";
const numberValue = 20;
const numberValue2= 20,000;
    OUTPUT error, doesnt need comma 
    big numbers we just write 20000 etc
const booleanValue = false;
    false/true, only 2 values

const firstArray = [10, 20, 30, 40];
console.log(firstArray[0])
    OUTPUT 10, like in python, the index is 0, 1, 2 etc
const secondArray = [10, 'a string', {prop: 'slkdfsl'}, [1, 2]];
console.log(secondArray[0])
    OUTPUT Array [ 1, 2 ]
console.log(secondArray[3][0]);
    OUTPUT  1